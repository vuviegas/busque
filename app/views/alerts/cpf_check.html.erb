<div class="container">
  <h4 class="mt-5 ml-1"> Cadastrar Suspeita</h4>
  <div class="card text-primary shadow-sm pb-3 bg-white rounded" style="width:400px;">
    <div class="card-body">
          <div>
            <%= simple_form_for @alert, url: cpf_check_path, method: :post do |f| %>
              <%= f.simple_fields_for :passenger do |passenger_form| %>
                <%= passenger_form.input :cpf, label: "CPF", maxlength: "14" %>
              <% end %>
              <div class="form-actions text-right">
                <%= f.button :submit, value: "Buscar", :class => "btn btn-primary btn-sm" %>
              </div>
            <% end %>
          </div>
        </div>
    </div>
</div>

<script>

</script>