<div class="container">
  <h4 class="mt-3 ml-1 mb-3 titulo-page text-center"> Cadastrar Empresa</h4>
  <div class="card text-primary shadow-sm bg-white rounded" style="width: 30vw; margin: 0 auto">
    <div class="card-body">
      <%= simple_form_for(@company) do |f| %>
      <div class="form-inputs">
        <%= f.input :company_name, label: "Nome da empresa" %>
      </div>
      <div class="form-inputs">
        <%= f.association :user, label: "Usuário", collection: User.where(role: 'clerk'), label_method: :email %>
      </div>
      <div class="form-actions text-right">
        <%= f.button :submit, value: "Salvar", :class => "btn btn-primary btn-sm" %>
      </div>
        <% end %>
    </div>
  </div>
</div>
